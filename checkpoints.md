# amyr
- [x] setup basic client servr communication
done, run `node server/server.ts` compile with `gcc main.c` and run `./a.out`
---
- [ ] send a file to server, have server save it
---
- [ ] setup a signal whenever a syncd directory is changed, if signal send to server
----

# brandon
